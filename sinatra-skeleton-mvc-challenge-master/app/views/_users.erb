<% all_users.each do |user| %>
  <%= user.username %>
  <% if current_user.idols.include?(user) %>
    <%= erb :_unfollow, locals: { user: user, current_user: current_user } %>
  <% else %>
    <%= erb :_follow, locals: { user: user, current_user: current_user } %>
  <% end %>
<% end %>
